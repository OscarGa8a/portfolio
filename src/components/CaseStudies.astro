---
import type { Locale } from '../i18n/index';
import { t } from '../i18n/index';
import { CldImage } from 'astro-cloudinary';
import CaseStudyCard from './CaseStudyCard.astro';

interface Props { lang: Locale; }
const { lang } = Astro.props;
---
<section id="casos" class="py-24 bg-bg-sec/30 border-y border-white/5 reveal">
  <div class="max-w-7xl mx-auto px-6 lg:px-8">
    <div class="max-w-3xl mb-12">
      <h2 class="font-display text-3xl md:text-4xl font-bold mb-5 text-text-main">{t(lang, 'cases_title')}</h2>
      <p class="text-text-sec text-lg">{t(lang, 'cases_subtitle')}</p>
    </div>

    <div class="divide-y divide-white/10 border-t border-white/10">
      <!-- Duarte's Auto Detailing (flagship · US client) -->
      <div class="py-14 first:pt-12">
        <CaseStudyCard
          lang={lang}
          title="Duarte's Auto Detailing"
          tagKey="cases_tag_detailing"
          descKey="cases_duartes_desc"
          features={['cases_duartes_f1', 'cases_duartes_f2', 'cases_duartes_f3']}
          problemKey="cases_duartes_problem"
          solutionKey="cases_duartes_solution"
          resultKey="cases_duartes_result"
          ctaLabelKey="cases_visit_web"
          ctaHref="https://duartesautodetailing.com/"
          variant="image-right"
        >
          <CldImage
            slot="mockup"
            src="oscar_dev/duartes_y9fonr"
            width={960}
            height={720}
            alt={lang === 'es' ? "Duarte's Auto Detailing — landing de detailing premium a domicilio" : "Duarte's Auto Detailing — premium mobile detailing landing"}
            class="w-full aspect-[4/3] object-cover object-top rounded-xl border border-white/10"
            loading="lazy"
          />
        </CaseStudyCard>
      </div>

      <!-- ConectaPlay -->
      <div class="py-14">
        <CaseStudyCard
          lang={lang}
          title="ConectaPlay"
          tagKey="cases_tag_sports"
          descKey="cases_conectaplay_desc"
          features={['cases_conectaplay_f1', 'cases_conectaplay_f2', 'cases_conectaplay_f3']}
          problemKey="cases_conectaplay_problem"
          solutionKey="cases_conectaplay_solution"
          resultKey="cases_conectaplay_result"
          ctaLabelKey="cases_visit_web"
          ctaHref="https://app.conectaplay.co/"
          variant="image-left"
        >
          <div slot="mockup" class="w-full flex justify-center py-2 sm:py-4">
            <div class="relative w-[260px] sm:w-[280px] md:w-[300px] aspect-[316/659] rounded-[2rem] sm:rounded-[2.5rem] p-2 bg-gradient-to-b from-white/15 via-white/5 to-white/10 border border-white/20 shadow-2xl shadow-black/80 ring-1 ring-black/40 group overflow-hidden">
              <div class="absolute top-3.5 left-1/2 -translate-x-1/2 w-16 h-3 bg-bg-main/90 rounded-full border border-white/10 z-20 pointer-events-none flex items-center justify-end px-2">
                <div class="w-1 h-1 rounded-full bg-accent/60"></div>
              </div>
              <CldImage
                src="oscar_dev/conecta_play_q9zbia"
                width={316}
                height={659}
                alt={lang === 'es' ? 'ConectaPlay — plataforma de gestión deportiva en versión móvil' : 'ConectaPlay — mobile sports management platform'}
                class="w-full h-full object-cover object-top rounded-[1.5rem] sm:rounded-[2rem] border border-white/5 transition-transform duration-500 ease-out group-hover:scale-[1.02]"
                loading="lazy"
              />
            </div>
          </div>
        </CaseStudyCard>
      </div>

      <!-- Upsteppers -->
      <div class="py-14">
        <CaseStudyCard
          lang={lang}
          title="Upsteppers"
          tagKey="cases_tag_streetwear"
          descKey="cases_upsteppers_desc"
          features={['cases_upsteppers_f1', 'cases_upsteppers_f2', 'cases_upsteppers_f3']}
          problemKey="cases_upsteppers_problem"
          solutionKey="cases_upsteppers_solution"
          resultKey="cases_upsteppers_result"
          ctaLabelKey="cases_visit_store"
          ctaHref="https://www.upsteppers.co/"
          variant="image-right"
        >
          <CldImage
            slot="mockup"
            src="oscar_dev/upsteppers_fwoojy"
            width={960}
            height={720}
            alt={lang === 'es' ? 'Upsteppers — tienda de ropa urbana premium y streetwear colombiano' : 'Upsteppers — premium streetwear online store'}
            class="w-full aspect-[4/3] object-cover object-top rounded-xl border border-white/10"
            loading="lazy"
          />
        </CaseStudyCard>
      </div>

      <!-- Fuera del Molde -->
      <div class="py-14">
        <CaseStudyCard
          lang={lang}
          title="Fuera del Molde"
          tagKey="cases_tag_ecom"
          descKey="cases_fueradelMolde_desc"
          features={['cases_fueradelMolde_f1', 'cases_fueradelMolde_f2', 'cases_fueradelMolde_f3']}
          problemKey="cases_fueradelMolde_problem"
          solutionKey="cases_fueradelMolde_solution"
          resultKey="cases_fueradelMolde_result"
          ctaLabelKey="cases_visit_store"
          ctaHref="https://fueradelmolde.com/"
          variant="image-left"
        >
          <CldImage
            slot="mockup"
            src="oscar_dev/fueradelmolde_y8vm23"
            width={960}
            height={720}
            alt={lang === 'es' ? 'Fuera del Molde — e-commerce de productos personalizados' : 'Fuera del Molde — custom products e-commerce'}
            class="w-full aspect-[4/3] object-cover object-top rounded-xl border border-white/10"
            loading="lazy"
          />
        </CaseStudyCard>
      </div>

      <!-- La Locura Bar -->
      <div class="py-14">
        <CaseStudyCard
          lang={lang}
          title="La Locura Bar"
          tagKey="cases_tag_bar"
          descKey="cases_locuraBar_desc"
          features={['cases_locuraBar_f1', 'cases_locuraBar_f2', 'cases_locuraBar_f3']}
          problemKey="cases_locuraBar_problem"
          solutionKey="cases_locuraBar_solution"
          resultKey="cases_locuraBar_result"
          ctaLabelKey="cases_visit_web"
          ctaHref="https://lalocurabar.com.co/"
          variant="image-right"
        >
          <CldImage
            slot="mockup"
            src="oscar_dev/la_locura_bar_owfcnw"
            width={960}
            height={720}
            alt={lang === 'es' ? 'La Locura Bar — sitio web del bar' : 'La Locura Bar — bar website'}
            class="w-full aspect-[4/3] object-cover object-top rounded-xl border border-white/10"
            loading="lazy"
          />
        </CaseStudyCard>
      </div>
    </div>

  </div>
</section>
